package com.romco.bracketeer.persistence.util

class Constants {
    companion object {
        // TODO remove, this is not needed anymore
        //  as the user will now have this as the default (and the only accessible) schema!
        const val UM_SCHEMA = "bracketeer_um"
    }
}